# Artefacts de build (regénérés par build.ps1 / build.sh)
src/build/
*.class
*.jar

# ...sauf le binaire de release versionné (pour télécharger sans compiler)
!dist/KamoofLite.jar

# Sauvegardes du jar serveur
*.bak
*.bak-*
*.bak.*

# Divers OS / éditeur
Thumbs.db
.DS_Store
